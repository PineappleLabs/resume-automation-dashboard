SESSION_SECRET_KEY=CHANGE_ME_run_python_-c_"import secrets;print(secrets.token_hex(32))"
DASHBOARD_PASSWORD=CHANGE_ME
HOST=127.0.0.1
PORT=8000
# ANTHROPIC_API_KEY is intentionally NOT set here -- app/config.py loads it directly from
# packages/resume_pipeline/.env so the key is never duplicated across two files.
# DATABASE_URL is intentionally NOT set here either -- jobsearch_db/config.py loads it
# directly from packages/jobsearch_db/.env, the canonical home shared with services/ingest_gmail.
