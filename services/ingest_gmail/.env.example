CLASSIFICATION_CONFIDENCE_THRESHOLD=0.75
TARGET_LOCATION_DESCRIPTION=Atlanta, Georgia, or fully remote
POLL_INTERVAL_MINUTES=30
FULL_RESYNC_DAYS=7
# DATABASE_URL lives in packages/jobsearch_db/.env (shared with services/api)
# ANTHROPIC_API_KEY lives in packages/resume_pipeline/.env (shared with the CLI/API)
